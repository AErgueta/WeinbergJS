<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Proforma</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 13px;
            margin: 40px;
            color: #000;
        }
        .row {
            display: flex;
            justify-content: space-between;
        }
        .section {
            margin-bottom: 20px;
        }
        .bold {
            font-weight: bold;
        }
        .box {
            border: 1px solid #000;
            padding: 10px;
            margin-top: 10px;
        }
        .footer {
            margin-top: 80px;
            display: flex;
            justify-content: space-between;
            font-size: 11px;
        }
    </style>
</head>
<body>

    <!-- Encabezado -->
    <div class="row section">
        <div>
            <div><span class="bold">Cliente:</span> {{customer.nombreCus}}</div>
            <div><span class="bold">Teléfono:</span> {{customer.telefonoCus}}</div>
        </div>
        <div style="text-align: right;">
            <div>La Paz, {{version.fecha}}</div>
            <div><span class="bold">Cód:</span> {{customer.alias}}</div>
        </div>
    </div>

    <!-- Detalle principal -->
    <div class="box">
        <div class="bold">Proforma</div>
        <table style="width: 100%; margin-top: 10px;">
            <thead>
                <tr>
                    <th style="text-align: left;">Cantidad</th>
                    <th style="text-align: left;">Producto</th>
                    <th style="text-align: right;">Precio en Bolivianos</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>{{detalle.cantidadQuo}}</td>
                    <td>
                        {{detalle.descripcionQuo}}
                    </td>
                    <td style="text-align: right;">
                        {{sum version.lineas "monto"}}
                    </td>
                </tr>
            </tbody>
        </table>
    </div>

    <!-- Pie de página -->
    <div class="footer">
        <div>
            Tiempo de entrega: 3 a 5 días hábiles.<br>
            Validez de la cotización: 15 días.
        </div>
        <div style="text-align: right;">
            Jorge Weinberg<br>
            Director<br>
            Página 1 de 1
        </div>
    </div>

</body>
</html>
